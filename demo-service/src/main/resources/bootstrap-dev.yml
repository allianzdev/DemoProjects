spring:
  cloud:
    config:
      discovery:
        service-id: demo-config-service
        enabled: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:10000/eureka/

server:
  port: 15000